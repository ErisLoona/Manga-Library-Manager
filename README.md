# Development Branch  
Here I will be working on migrating the app to the MAUI framework to enable native cross-platform. First I will try recreating the UI as faithfully as I can, then I will refactor and rewrite the code step by step. Once this is finished it will replace the old WinForms version. The upgrade will be seamless.
